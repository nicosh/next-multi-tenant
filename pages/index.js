import React from 'react'
import RenderPage from '../tenant/components/renderPage'
const Home = ({ websiteData,isSsr }) => <RenderPage page="index" websiteData={websiteData} isSsr={isSsr} />
export default Home

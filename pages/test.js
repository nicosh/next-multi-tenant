import React from 'react'
import RenderPage from '../tenant/components/renderPage'
const Test = ({ websiteData,isSsr }) => <RenderPage page="index" websiteData={websiteData} isSsr={isSsr} />
export default Test
